{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. Only core IPC; all network access goes through the app's own Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
